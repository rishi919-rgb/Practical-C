#include <stdio.h>
int main(){
      //POsi Negi or 0
    int num = 7;
    if (num > 0)
    {
        printf("Positive");
    }
    else if (num < 0)
    {
        printf("negative");
    }
    else
    {
        printf("zero");
    }
}